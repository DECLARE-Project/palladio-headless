package de.fabiankeller.palladio.builder.repository;

/**
 * Defines all available data types for parameters.
 * <p>
 * Used by {@link SignatureBuilder} to specify signatures.
 */
public enum ParameterType {
    BOOL,
    BYTE,
    CHAR,
    DOUBLE,
    INT,
    STRING,
}
