<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.fabiankeller.palladio-headless</groupId>
        <artifactId>palladio-headless</artifactId>
        <version>0.0.1</version>
    </parent>

    <artifactId>palladio-bridge</artifactId>
    <packaging>eclipse-plugin</packaging>

    <name>Palladio Headless Bridge</name>
    <description>Provides all Palladio dependencies and ensures they work outside an OSGi environment.</description>

    <repositories>
        <repository>
            <id>PalladioSimulator</id>
            <layout>p2</layout>
            <url>https://sdqweb.ipd.kit.edu/eclipse/palladiosimulator/releases/1.0.7/</url>
        </repository>
        <repository>
            <id>Eclipse Luna</id>
            <layout>p2</layout>
            <url>http://download.eclipse.org/releases/mars/</url>
        </repository>
    </repositories>

    <build>
        <plugins>
            <plugin>
                <!-- this is awesome: http://stackoverflow.com/a/6710870/1262901 -->
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>tycho-maven-plugin</artifactId>
                <version>0.25.0</version>
                <extensions>true</extensions>
            </plugin>
            <plugin>
                <groupId>org.eclipse.tycho</groupId>
                <artifactId>tycho-packaging-plugin</artifactId>
                <version>0.25.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/dependency</outputDirectory>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.2.1</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/resources/assembly-repackaged.xml</descriptor>
                    </descriptors>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>